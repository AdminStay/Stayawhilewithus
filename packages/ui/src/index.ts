// Shared React components are added as real UI needs emerge in later
// phases (Dashboard Foundation onward). Intentionally empty for now —
// apps/website uses plain Tailwind-styled elements in the Phase 1
// vertical slice.
export {};
